#include "minishell.h"

int init_pipe(int **p)
{}

int execute_cmd(t_c *c, t_d *d)
{
    int pipeid[MAXMM][2];
    int exits;

    if valid_check(c)
    if (!init_pipe(pipeid))
        return (0);
    while (c)
    {
        
        c = c->next;
    }
}